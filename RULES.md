# 1_44MB プロジェクトの実装ルール

プロジェクトに変更を加える前に、毎回このファイルを確認すること。

## フォルダと出力先

- 作成した.exeファイルは基本的にbuildフォルダにまとめる。必要なdllファイルもそこにまとめる。
- srcフォルダのソースコードはEditor、Play、Sheredフォルダに分け、必要に応じてさらに分化させる。
- 参照する外部ファイルは`assets`配下で種類ごとに別のフォルダへまとめる。`build`フォルダにはスプライトなどの外部ファイルを入れない。

## 実装とコメント

- 設置入力は設置物の種類から独立させ、設置物ごとの定義・処理を登録して共通の設置処理から呼び出す。設置物を追加・変更しても、入力処理を個別に分岐・複製しない。
- ブロックモードで配置できるものは、原則として固有ID・種類・位置・必要な設定を持つ配置オブジェクトとして管理する。マス値だけに個別の状態を持たせず、共通の配置・選択・移動・削除・保存・履歴処理へ登録して扱う。

- 責務ごとにファイルを分割する。敵、UI、プレイヤー、ステージ、保存処理などを一つのファイルへ混在させない。
- 一つの機能ごとに関数を作る。`main()` は初期化・ゲームループ・終了処理の呼出しにとどめ、長くしない。
- 長い処理や意図が読み取りにくい処理の直前には、日本語のコメントで目的と処理内容を説明する。重要な状態や分岐にも必要に応じて説明を加える。
- 既存のシステム・関数・データ構造で実現できる機能は再利用し、同等の処理やデータを重複して実装しない。容量とスクリプト量を抑える。
- 保存形式・配置方法・データ構造などのシステムを変更した時は、既存データとの整合性を確認し、必要に応じて移行・初期化・不要データの削除を行う。
- 配置・削除・設定変更など永続化すべき状態を追加・変更する機能は、保存システムと必ず連携する。保存、復元、未保存表示、ゲーム起動時の読み込みまで確認し、編集画面だけで状態が完結しないようにする。
- 今後追加するブロック・アイテム・ブロックの性質に関するデータは、既存の全体保存システムへ統合する。個別の自動保存や個別保存ボタンは作らず、`S`・`Revert saved`・終了時確認で同じように扱う。
- マップ上へ配置するイベントも、既存の全体保存システムへ統合する。
- 履歴を保存する場合は、履歴を復元するために必要な変更前の最小要素だけを記録し、状態全体の複製は避ける。
- 類似した機能は、操作方法・画面遷移・戻る動作を統一する。差が必要な場合だけ、理由が分かる形で個別の操作を追加する。
- 新しいテキスト入力欄は、既存の共通関数`UpdateTextInput`と`DrawTextCaret`を必ず使う。個別の入力・カーソル処理を新設せず、UTF-8のカーソル移動・範囲選択・置換・削除・日本語IME表示・カーソル表示を会話入力と同じ操作・見た目に統一する。
- モーダルやインスペクターなど独立したUIへのクリックは、そのUIだけで処理して背面のUI・マップ操作へ伝播させない。
- パレット・一覧・ボタンなど独立したUIを押した後は、ボタンを離すまでの押下継続と解放もそのUIで消費し、背面のドラッグ・配置・削除などの操作を開始させない。
- 表示要素数が固定でない一覧・枠には、内容が表示領域を超えた場合に確認できるスクロール操作を実装する。
- 日本語の表示文字を追加・変更した時は、ゲーム用フォントの文字セットへ事前登録できているか確認し、未登録文字による`?`表示を防ぐ。

## 依存関係とイベント

- 新規ファイルの先頭には、そのファイルが依存する自プロジェクト内ファイルを日本語コメントで記載する。依存関係を変更した場合も該当箇所にコメントを加える。
- 新規ファイルの追加または依存関係の変更時は、チャットで依存方向を報告する。
- 複数システムに反応させる必要がある場合、または依存方向と逆向きに通知する必要がある場合にだけイベントを使う。イベントを追加・変更した場合は、発行者と購読者をチャットで報告する。

## 変更後の確認

- 変更後は、警告を有効にした状態でビルドし、実行ファイルを更新する。
- 操作や実装に変更があった時は、READMEに記載し、更新する。

## オブジェクトフォルダ

- オブジェクト・弾・設置物の外部ファイルは、共通の所有フォルダとして管理する。フォルダ名にはオブジェクト名、種類番号、識別番号、位置など必要な情報を含める。
- 所有フォルダはオブジェクトの生成から消滅までだけ存在させ、Zipper の操作ではコピーや同期を行わず、同じフォルダ本体を移動する。
- メタ情報だけのブロックは所有フォルダを生成しない。外部ファイルを持つオブジェクトが消滅する場合は、ファイルを File.png のドロップへ引き継ぐ。
- メタ情報だけの通常ブロックも仮想オブジェクトとして扱い、Zipper 操作時だけ同じ命名規則の実フォルダを一時的に生成して編集可能にする。
- 新しいオブジェクトや弾も、この共通の所有・移動・消滅処理を再利用する。

## README

- READMEは機能の目的・操作方法・設定箇所だけを簡潔に記載する。キーやクリックによる操作方法は記載する一方、会話の行順表示などの説明不要な挙動は記載しない。
